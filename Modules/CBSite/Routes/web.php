<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

//Route::prefix('cbsite')->group(function() {
//    Route::get('/', 'CBSiteController@index')->name('cb-site');
//});
//Route::get( '/{all}/{all1}', 'CBSiteController@all')->where('all', '^((?!admin).)*$');
//Route::get( '/{all}/{all1}/{all2}', 'CBSiteController@all')->where('all', '^((?!admin).)*$');
//Route::get(setting('post_permalink','post/{id}'), function (){
////    return 'asd';
//})->name('frontend.postDetailWithCatAndName')->middleWare('next-request');
//Route::any( '/{all?}', 'CBSiteController@all')->where('all', '^((?!admin).)*$')->where('all', '^((?!imba).)*$');
